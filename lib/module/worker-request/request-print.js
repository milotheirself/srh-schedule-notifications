const fragment = {};
const internal = {};

fragment.requestImage = async ({ schadule }) => {
  // // ---
  // nodeFiles.mkdirSync(outputs(`schedule-screenshot/`), { recursive: true });
  // // ---

  // // ---
  // // await page.waitForSelector('center > table');
  // // const element = await page.$('center > table');
  // // await element.screenshot({ path: outputs(`screenshot/${i}-${j}.png`), fullPage: true });
  // await page.screenshot({ path: outputs(`schedule-screenshot/${i}-${j}.png`), fullPage: true });
  // // ---

  console.log(schadule);
  return {};
};

export default { ...fragment };
